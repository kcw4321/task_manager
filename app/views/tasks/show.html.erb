<h1>Task</h1>
<h2><%= @task.name %></h2>
<p>Stop being lazy and do this!</p>
<%# need @ in () %>
<%= link_to 'Delete', task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<%= link_to 'Edit', tasks_path(@task) %>
<%= link_to 'Back to all tasks', tasks_path %>
